<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{store.name}} Queue</title>
</head>
<body>
    {{#if currentUser}}
    Join Queue<br>
    <button type="submit" class="btn btn-primary">Join Queue</button>
    {{/if}}
    {{#if store}}
    test
    {{store}}
    {{store.name}}
    {{/if}}
</body>
</html>